;; Assert some facts
assertz((parent john mary)).
assertz((parent mary tom)).
assertz((parent tom anna)).

;; Assert the grandparent rule
assertz(((grandparent X Y) :- (parent X Z) (parent Z Y))).

;; Assert a fact that contradicts the existing facts (flaw)
assertz((grandparent john anna)).

;; Run a query
?- (grandparent X tom)

;; Another query
?- (grandparent mary X)

;; List all clauses
list.